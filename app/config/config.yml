imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

framework:
    #esi:             ~
    translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: gmail
    host:      gmail.com
    username:  iut.bibliotheque
    password:  projet2015


# app/config/config.yml
fos_user:
    db_driver:     orm
    firewall_name: main
    user_class:    Biblio\UserBundle\Entity\User
    service:
        mailer: fos_user.mailer.twig_swift
    registration:
        form:
            type: biblio_user_registration
        confirmation:
            enabled: true
            from_email:
                address:        registration@acmedemo.com
                sender_name:    Enregistrement Bibliothèque
            template: BiblioGeneralBundle:Default:registration.email.twig
    resetting:
        email:
            from_email:
                address:        resetting@acmedemo.com
                sender_name:    Mot de passe oublié : Bibliothèque
            template: BiblioGeneralBundle:Default:resetting.email.twig
genemu_form:
    recaptcha:
        public_key: 6Le3HgUTAAAAAEoE3P7dKbkIth5SFZeND5kfwgE1
        private_key: 6Le3HgUTAAAAAM-v5gts_2mYpU_ZNYD1JORl5vkj
    tinymce:
        enabled: true
        theme:   modern
        configs:
            language: fr_FR
    autocomplete: ~